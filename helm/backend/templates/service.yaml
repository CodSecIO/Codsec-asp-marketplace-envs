apiVersion: v1
kind: Service
metadata:
  name: {{ include "asp-backend.name" . }}
  labels: {{- include "asp-backend.labels" . | nindent 4 }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    app.kubernetes.io/name: {{ include "asp-backend.name" . }}
